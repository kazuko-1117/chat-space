.MainMessages{data: {message: {id: message.id}}}
  .MainMessages__Top 
    .MainMessages__Name
      = message.user.name
    .MainMessages__Time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .MainMessages__Text
    - if message.content.present?
      %p.MainMessage__content
        = message.content
    = image_tag message.image.url, class: 'MainMessage__image', width: '424px' if message.image.present?
